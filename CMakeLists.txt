cmake_minimum_required(VERSION 3.22.1)
project("fxcam")

# Placeholder for future Vulkan/native shader compilation
# add_library(fxcam-native SHARED fxcam.cpp)
# find_library(vulkan-lib vulkan)
# target_link_libraries(fxcam-native ${vulkan-lib} android log)
